#ifndef REVERSE_H
#define REVERSE_H
#include <math.h>

#include <string>

using namespace std;

class Reverse {
 public:
  int reverseDigit(int value);
  string reverseString(string letters);
};
#endif
